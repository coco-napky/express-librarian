'use strict';

//abstract repository base class
class Repository {

  constructor(model) {
    this.model = model;
  }

  getModels() {
  }

  getModelById(id) {
  }

  postModel(name){
  }

  deleteModel(id) {
  }
}

module.exports = Repository;
